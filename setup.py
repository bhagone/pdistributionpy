from setuptools import setup

setup(name='bdistributionpy',
      version='0.1',
      description='Gaussian distributions and binomial distribution',
      packages=['bdistributionpy'],
      zip_safe=False)
